
  Home', administrator_admins_path, class: 'btn' %>
  nk_to 'Manage Categories', administrator_categories_path, class: 'btn' %>
  <%= link_to 'Manage Orders', administrator_orders_path, class: 'btn' %>
  <%= link_to 'Manage Users', administrator_users_path, class: 'btn' %>

  <% @items.each do |item| %>
    <tr>
      <%= image_tag image_or_fallback_admin(item) %>
      <%= item.title %>
      <%= item.description %>
      <%= number_to_currency(item.price) %>
      <%= item.current_status %>
      <% item.categories.each do |category| %>
         <%= category.title %>
      <% end %>

      <%= link_to 'Edit', edit_administrator_item_path(item) %>
      <%= link_to 'Delete', administrator_item_path(item), method: :delete, data:{
        confirm: "Are you sure you want to delete the item: '#{item.title}'" } %>
  <% end %>

  <%= link_to 'Create New Item', new_administrator_item_path %>
