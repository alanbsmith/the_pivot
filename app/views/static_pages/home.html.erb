<% content_for :title do %>About Us<% end %>
<% content_for :description do %>This page will display information about our online ordering site.
<% end %>

<div class="container-fluid" id="selector_1">
  <% if signed_in? %>
  <%= link_to "Signout", signout_path, method: "delete" %>
  <% end %>
  <div class="container">

    <div class="row">
      <div class="col-md-4 col-md-offset-1">
        <!-- <div class="box_front_text">.</div>
        <p class="front_text text-justifY"> Scream’n Cream aims to put the romance back in ice cream. We fuse custom made, one of a kind ice screams with adult favorites: Tequila, Whiskey, Liquor, Beer, Wine, and Rum to create grown up treats that are out of this world. We use only the freshest ingredients in the crafting of our screams and sorbets so that you know that each delicious spoonful is the very best you can get.</p> -->
      </div>

      <% if current_user %>
      <div class="col-md-5 col-md-offset-2 user_widget">
        <div>
          <div id="the_gravatar"><%= gravatar_for @user %></div>

          <p class="hello_customer xxl_hello"><%= "Hi #{current_user.first_name}!" %></p>
          <p class="hello_customer l_hello push_up"><%= "Welcome back!" %></p>
          <p class="hello_companion"> <%= "There are #{ @cart.total_items } booze ice creams waiting for you!" %></p>

          <%= link_to cart_path(@cart), class: "btn btn-warning full top_hello", id: "hello_customer" do  %>
          Complete your Order <span class="glyphicon glyphicon-shopping-cart"></span>
          <% end %>

          <div class="row">
            <%= link_to items_path, class: "col-md-4 col-md-offset-1  btn btn-warning", id: "hello_customer" do  %>
              Contact Us
            <% end %>

            <%= link_to items_path, class: "col-md-4 col-md-offset-2  btn btn-warning", id: "hello_customer" do  %>
              Menu
            <% end %>
          </div>


        </div>
      </div>

      <% else %>
      <p style="color:red">LOGIN SECTION</p>
      <% end %>
    </div>

    <div class="row">
      <div class="col-md-5 col-md-offset-7 logo"></div>

    </div>

    <div class="row">
      <div class="col-md-5 col-md-offset-7">
        <div class="box_front_text">.</div>
        <p class="front_text text-justifY"> Scream’n Cream aims to put the romance back in ice cream. We fuse custom made, one of a kind ice screams with adult favorites: Tequila, Whiskey, Liquor, Beer, Wine, and Rum to create grown up treats that are out of this world. We use only the freshest ingredients in the crafting of our screams and sorbets so that you know that each delicious spoonful is the very best you can get.</p>
      </div>
    </div>



  </div>
</div>
<!-- <p class="map_text find_area text-left">Find your closest amusing taste!</p> -->
<div id="map-canvas" class="col-md-12 map_box"></div>



<!--
    <div class="row">
      <% @categories.each do |category| %>
      <div class="col-md-3 category_box">
        <div class="col-md-12 text-center xl_hello"><%= category.title %></div>
      </div>
      <% end %>
    </div>

    <% @items.each do |item| %>
    <tr>
      <td><%= image_tag item.image.url(:user_thumb) %></td>
      <td><%= link_to item.title, item_path(item) %></td>
      <td><%= item.description %></td>
      <td><%= number_to_currency(item.price) %></td>
      <td>
        <%= button_to "Add To Cart", cart_items_path(item_id: item) %>
      </td>
    </tr>
    <% end %>

  </div>
</div> -->

<% if notice %>
  <%= notice %>
<% end %>
