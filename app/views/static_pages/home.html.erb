<% if notice %>
  <%= notice %>
<% end %>

<div class="jumbotron">
  <h1> EmployMe </h1>
  <h3 class="text-muted">find your next great opportunity</h3>
  <% unless signed_in? %>
    <a class="btn btn-primary"<%= link_to "Register Your Business", new_user_path %></a>
  <% end %>
</div>
